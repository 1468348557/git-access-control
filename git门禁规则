Git 门禁规则

---

## 触发方式

| 事件 | 触发检查 |
|---|---|
| Push（推送代码） | 仅分支命名校验 |
| MR（创建合并请求） | 分支命名 + 合并方向 + 变更行数 + 基线校验 + 人工审批 |

---

## 1. 分支命名校验

所有 push 和 MR 事件均触发。分支名必须匹配以下格式，且日期部分必须真实存在：

| 类型 | 正则 | 示例 |
|---|---|---|
| FIX（缺陷修复） | `^FIX-[0-9]{8}-[0-9]{4}$` | `FIX-20260513-0001` |
| REQ（需求） | `^REQ-[0-9]{8}-[0-9]{4}$` | `REQ-20260513-0001` |
| PUB（发布） | `^PUB-[0-9]{8}-[0-9]{4}$` | `PUB-20260513-0001` |
| hotfix | `^hotfix-[0-9]{8}$` | `hotfix-20260513` |
| release | `^release-[0-9]{8}$` | `release-20260513` |

---

## 2. 合并方向校验（仅 MR）

源分支 → 目标分支必须在白名单内：

| 源分支 | 目标分支 |
|---|---|
| FIX / REQ / PUB | `uat` 或 `uat_YYYYMMDD` |
| hotfix | `master` |
| release | `master` |

---

## 3. MR 变更行数校验（仅 MR）

- 计算源分支相对于目标分支的变更行数（新增 + 删除）
- ≤ 100 行：自动通过，写入 `PASS` 标记
- > 100 行：写入 `NEEDS_APPROVAL` 标记，触发 Stage 2 人工审批

---

## 4. 合并前基线校验（仅 MR）

检查源分支是否包含目标分支的最新提交：

| 合并方向 | 是否检查 | 说明 |
|---|---|---|
| FIX/REQ/PUB → uat | 跳过 | 业务分支基于 master，与 uat 无继承关系 |
| hotfix → master | 检查 | 源分支必须包含 master 最新提交 |
| release → master | 检查 | 源分支必须包含 master 最新提交 |

---

## 5. 人工审批（仅 MR）

流水线 Stage 2 为 `when: manual` 手动审批任务：

- 读取 Stage 1 生成的 `approval_status` 文件
- `PASS`：无需审批，点击即过
- `NEEDS_APPROVAL:<行数>`：审批人确认变更后通过
