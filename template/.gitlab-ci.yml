stages:
  - gate

variables:
  GIT_DEPTH: "0"

# 关键分支 MR（目标为 hotfix/release/master）使用专用 runner
gate:critical:
  stage: gate
  tags:
    - shell-critical
  script:
    - echo "CI_PIPELINE_SOURCE=$CI_PIPELINE_SOURCE"
    - echo "CI_COMMIT_REF_NAME=$CI_COMMIT_REF_NAME"
    - echo "CI_MERGE_REQUEST_SOURCE_BRANCH_NAME=$CI_MERGE_REQUEST_SOURCE_BRANCH_NAME"
    - echo "CI_MERGE_REQUEST_TARGET_BRANCH_NAME=$CI_MERGE_REQUEST_TARGET_BRANCH_NAME"
    - rm -rf /tmp/gate/scripts
    - mkdir -p /tmp/gate/scripts
    - curl -sS -o /tmp/gate/scripts/git_gate.sh "${GATE_URL}/scripts/git_gate.sh"
    - chmod +x /tmp/gate/scripts/git_gate.sh
    - /tmp/gate/scripts/git_gate.sh
  only:
    refs:
      - merge_requests
    variables:
      - $CI_MERGE_REQUEST_TARGET_BRANCH_NAME =~ /^hotfix-/
      - $CI_MERGE_REQUEST_TARGET_BRANCH_NAME =~ /^release-/
      - $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "master"

# 常规 Push + 非关键分支 MR
gate:
  stage: gate
  tags:
    - shell
  script:
    - echo "CI_PIPELINE_SOURCE=$CI_PIPELINE_SOURCE"
    - echo "CI_COMMIT_REF_NAME=$CI_COMMIT_REF_NAME"
    - echo "CI_MERGE_REQUEST_SOURCE_BRANCH_NAME=$CI_MERGE_REQUEST_SOURCE_BRANCH_NAME"
    - echo "CI_MERGE_REQUEST_TARGET_BRANCH_NAME=$CI_MERGE_REQUEST_TARGET_BRANCH_NAME"
    - rm -rf /tmp/gate/scripts
    - mkdir -p /tmp/gate/scripts
    - curl -sS -o /tmp/gate/scripts/git_gate.sh "${GATE_URL}/scripts/git_gate.sh"
    - chmod +x /tmp/gate/scripts/git_gate.sh
    - /tmp/gate/scripts/git_gate.sh
  only:
    refs:
      - pushes
      - merge_requests
  except:
    variables:
      - $CI_MERGE_REQUEST_TARGET_BRANCH_NAME =~ /^hotfix-/
      - $CI_MERGE_REQUEST_TARGET_BRANCH_NAME =~ /^release-/
      - $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "master"
